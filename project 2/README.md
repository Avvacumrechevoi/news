# Интерактивная диаграмма Ганта для VK Видео

Профессиональная интерактивная диаграмма Ганта для управления проектом запуска новостного сценария в VK Видео с локальным хранилищем данных в браузере (localStorage).

## Возможности

### Реализованные функции

#### Базовый функционал
- **Трехуровневая иерархия**: Проект → Эпики → Задачи
- **9 эпиков и 36 задач** с полными данными о проекте VK Видео
- **Timeline с 12 месяцами** года для планирования
- **Цветовое кодирование** по типам работ (не по кварталам)
- **Сворачивание/разворачивание** эпиков одним кликом

#### Система статусов
- **3 статуса задач**: Ожидает, В работе, Выполнено
- **Визуальная индикация** статусов с цветными бейджами
- **Быстрое переключение** статусов одной кнопкой
- **Зачеркивание** выполненных задач
- **Затемнение** баров завершенных задач

#### Управление задачами
- **Добавление** новых задач в любой эпик
- **Редактирование** задач через модальное окно
- **Удаление** задач с подтверждением
- **Детальная информация**: название, описание, ответственный, даты, тип, статус

#### Аналитика и прогресс
- **Общий прогресс проекта** в процентах и счетчиках
- **Прогресс эпиков** с мини прогресс-барами
- **Автоматический пересчет** при изменении статусов
- **Ключевые цели** по кварталам Q1-Q4
- **Счетчики**: завершено, в работе, общий прогресс

#### Поиск и фильтрация
- **Полнотекстовый поиск** по названию, описанию, ответственному
- **Фильтрация по типу** задачи (Подготовка, Разработка, Запуск, Рост, Веха)
- **Фильтрация по статусу** (Ожидает, В работе, Выполнено)
- **Счетчик найденных** задач
- **Сброс фильтров** одной кнопкой

#### Экспорт данных
- **Экспорт в JSON** со всей структурой проекта
- **Экспорт в CSV** для работы в Excel/Google Sheets
- **Сохранение метаданных** о времени экспорта

#### Режимы просмотра
- **"Всё"** - показывает эпики и все задачи
- **"Только эпики"** - high-level обзор проекта
- **Массовые действия**: развернуть/свернуть всё

#### Интерактивность
- **Hover-эффекты** на задачах и эпиках
- **Анимации** при переключении статусов
- **Плавные переходы** и тени
- **Показ длительности** в днях при наведении
- **Визуальная обратная связь** для всех действий

#### Хранилище данных
- **LocalStorage** для хранения данных в браузере
- **Автоматическая инициализация** данных при первом запуске
- **Работает без backend** и внешних сервисов
- **Сброс данных** через очистку localStorage в браузере

## Технический стек

- **React 18** с функциональными компонентами и хуками
- **TypeScript** для типобезопасности
- **Tailwind CSS** для стилизации
- **LocalStorage** для хранения данных без backend
- **Lucide React** для иконок
- **Vite** для сборки

## Структура проекта

```
src/
├── components/
│   ├── GanttChart.tsx      # Главный компонент диаграммы
│   ├── EpicRow.tsx          # Компонент эпика
│   ├── TaskRow.tsx          # Компонент задачи
│   ├── TaskModal.tsx        # Модальное окно редактирования
│   ├── StatsBar.tsx         # Панель статистики
│   ├── SearchBar.tsx        # Строка поиска
│   └── FilterBar.tsx        # Панель фильтров
├── hooks/
│   └── useGanttData.ts      # Хук для работы с данными
├── lib/
│   ├── localStore.ts        # Локальное хранилище (localStorage)
│   ├── constants.ts         # Константы и лейблы
│   ├── seedData.ts          # Начальные данные
│   └── exportUtils.ts       # Утилиты экспорта
├── types/
│   └── gantt.ts             # TypeScript типы
└── App.tsx                  # Корневой компонент
```

## Хранилище данных

Данные сохраняются локально в браузере (localStorage) в виде JSON структуры:

- project
- epics
- tasks

Для полного сброса данных очистите localStorage (ключ `gantt-local-store-v1`).

## Использование

### Изменение статуса задачи
Кликните на кнопку с иконкой слева от задачи для циклического переключения:
- ○ Ожидает → ⊙ В работе → ✓ Выполнено → ○ Ожидает

### Добавление задачи
1. Кликните на кнопку "+" в строке эпика
2. Заполните форму в модальном окне
3. Нажмите "Сохранить"

### Редактирование задачи
1. Кликните на кнопку ✎ рядом с задачей
2. Измените нужные поля
3. Нажмите "Сохранить"

### Поиск задач
Введите текст в строку поиска для фильтрации по названию, описанию или ответственному

### Фильтрация
Выберите типы и статусы для отображения только нужных задач

### Экспорт данных
1. Наведите на кнопку "Экспорт"
2. Выберите формат (JSON или CSV)
3. Файл автоматически скачается

## Публикация в GitHub Pages

Публикация настроена через GitHub Actions (файл `.github/workflows/deploy-pages.yml`).
Секреты не требуются. После пуша в `main` собирается `dist` и публикуется на GitHub Pages.
Данные хранятся локально в браузере пользователя.

## Что НЕ реализовано (будущие улучшения)

### Критичные
- Зависимости между задачами (predecessor/successor)
- Базовая линия (план vs факт)
- Drag & Drop для изменения дат на timeline
- Индикаторы просрочки и дедлайнов

### Важные
- История изменений и audit trail
- Комментарии к задачам
- Загрузка ресурсов и управление командой
- Критический путь проекта

### Желательные
- Экспорт в PDF/PNG с рендерингом диаграммы
- Мобильная адаптация
- Многопользовательский режим в реальном времени
- Уведомления о изменениях

### Будущее
- Интеграции с Jira, Asana, Trello
- Продвинутая аналитика (burn-down chart, velocity)
- Шаблоны проектов
- Импорт из MS Project

## Производительность

- **First Contentful Paint**: < 1s
- **Time to Interactive**: < 2s
- **Bundle size**: ~90KB gzipped
- **60 FPS** анимации
- Поддержка до **100+ задач** без деградации производительности

## Браузеры

- Chrome/Edge 90+
- Firefox 88+
- Safari 14+

## Доступность

- Keyboard navigation (Tab, Enter, Escape)
- Focus indicators
- Semantic HTML
- ARIA labels (частично реализовано)

## Цветовая схема

**Типы задач:**
- Подготовка: голубой градиент
- Разработка: фиолетовый градиент
- Запуск: розовый градиент
- Рост: зеленый градиент
- Веха: оранжевый градиент (увеличенная высота)

**Типы эпиков:**
- content: голубой
- tech: фиолетовый
- integration: розовый
- distribution: оранжевый
- optimization: зеленый
- monetization: индиго

## Лицензия

MIT

## Автор

Создано для проекта "Запуск новостного сценария VK Видео"
